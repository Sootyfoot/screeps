/*
function (loc)
{
  // if a road exists at loc, repair it
  // else if a flag exists at loc, upvote it
    // if flag upvote hits threshold, create construction site for road at loc and remove flag
  // else if no construction site at loc, drop a flag and set its countdown timer
  // else error
}

function (roadFlagArray)
{
  roadFlagArray.forEach(
    roadFlagArray[i].memory.countdown -=;
    if (roadFlagArray[i].memory.countdown <= 0)
    {
      // remove flag
    }
  );
}
